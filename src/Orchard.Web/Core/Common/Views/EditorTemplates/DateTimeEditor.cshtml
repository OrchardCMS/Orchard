@model Orchard.Core.Common.ViewModels.DateTimeEditor

@{
    Style.Require("DateTimeEditor");
}

@if (Model.ShowDate) {
    <label for="@Html.FieldIdFor(m => Model.Date)">@T("Date")</label>
    <span class="date inline">@Html.TextBoxFor(m => m.Date, new { placeholder = Model.DatePlaceholder ?? T("Date").Text, @class = "form-control" })</span>
}

@if (Model.ShowTime) {
    <label for="@Html.FieldIdFor(m => Model.Time)">@T("Time")</label>
    <span class="time inline">@Html.TextBoxFor(m => m.Time, new { placeholder = Model.TimePlaceholder ?? T("Time").Text, @class = "form-control" })</span>
}

@if (Model.ShowDate) { <text>@Html.ValidationMessageFor(m => m.Date)</text> }
@if (Model.ShowTime) { <text>@Html.ValidationMessageFor(m => m.Time)</text> }