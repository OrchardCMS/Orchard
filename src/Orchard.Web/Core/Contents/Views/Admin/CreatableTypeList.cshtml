@{ Layout.Title = T("Create New Content").ToString(); }

@foreach (var type in Model.ContentTypes) {
    <p>@Html.ActionLink((string)T(type.DisplayName).Text, "Create", new { Area = "Contents", Id = (string)type.Name, ContainerId = Model.ContainerId, ReturnUrl = Request.QueryString["ReturnUrl"] })</p>
}