@using Orchard.Core.Shapes
@{
    var editor = New.EditorBoolean();
    ShapeTemplateHelper.ForwardTemplateContextToShape(Html, ViewContext, editor, T("The value '{0}' is invalid.").Text);
}
@Display(editor)