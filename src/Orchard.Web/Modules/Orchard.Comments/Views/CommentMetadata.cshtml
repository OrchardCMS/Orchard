@*
    Model:
      ContentPart
*@

@using Orchard.Comments.Models;
@using Orchard.Utility.Extensions;
@{
    var comment = (Orchard.Comments.Models.CommentPart)Model.ContentPart;
}

@T("said") <time datetime="@comment.Record.CommentDateUtc.GetValueOrDefault()">@Display.Link(Value: Display.DateTimeRelative(DateTimeUtc: comment.Record.CommentDateUtc.GetValueOrDefault()), Href: "#comment-" + comment.Id)</time>