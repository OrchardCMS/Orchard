@{
    var model = (object)Model.Model;
    var templateName = "EditorTemplates/TaskSettings/" + (string)Model.TemplateName;
    var prefix = (string) Model.Prefix;

    ViewData.TemplateInfo.HtmlFieldPrefix = prefix;
}
@Html.Partial(templateName, model)